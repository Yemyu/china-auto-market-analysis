# 2026-08-25

## 冷启动改进（已回退）
- 读完 docs/PROJECT_HANDOFF.md，评估 DeepSeek 列的三项待办，补充了「生命周期年龄特征」建议。
- 实现了模块①冷启动「分段收缩」（scripts/36：category×价格带分层对数中位数 + n/(n+k) 收缩 + P10-P90 截断）。
- 关键发现：验证集上平铺中位数（HORIZON_MEDIAN）在所有指标都赢分段收缩；分段收缩只在测试 9 车系的「最差/均值」上更好（ES90 3121%→2090%、凯威德/海豹07EV 几乎命中），但全局加权更差（105% vs 90%）。
- 用户决定回退到老方法（平铺中位数）。已用 git show 恢复 scripts/36 内容并改回文件头，重跑输出，38.64% 等旧数字全部还原。
- 冷启动在 README 里只占 1 行表格 + 1 句话，看板无冷启动页面，篇幅极小。

## 项目状态
- 用户决定暂停项目。工作区有上一版模型遗留的大量未提交 rename/delete/modify，尚未 commit。
