# 2026-08-26

## 对外文档隐私清理（用户指出）
- 用户发现 README 快速开始里有 `conda activate nlp-sentiment`，违反其红线（对外文档不出现个人环境名）。
- 已清理 5 处：README.md、README_EN.md 的快速开始（conda 命令 → `pip install -r requirements.txt`，并删除"python scripts/<script>.py"常识性说明）；data/README.md、data/README_EN.md 第 7 节（同样改为 pip 口径）；environment.yml 的 `name:` 从 nlp-sentiment 改为 china-auto-market-analysis。
- 复查确认公开文件零残留（docs/ 已 gitignore 不算）。requirements.txt 已核对完整（environment.yml 只是 python3.13+requirements.txt 包装，看板是纯静态无需 Flask）。
